% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_elevation_data.R
\name{get_elevation_data}
\alias{get_elevation_data}
\title{Retrieve Elevation Data Along a Route}
\usage{
get_elevation_data(
  name,
  origin,
  destination = "chaesalp, switzerland",
  structure = "route",
  mode = "bicycling",
  cache_dir = "cache/elevation"
)
}
\arguments{
\item{name}{Character string identifying the route (e.g. a persons name). The value is
included in the output data frame.}

\item{origin}{Character string specifying the route starting location.}

\item{destination}{Character string specifying the route destination.
Defaults to \code{"chaesalp, switzerland"}.}

\item{structure}{Character string passed to \code{\link[ggmap:trek]{ggmap::trek()}} defining the
route structure. Defaults to \code{"route"}.}

\item{mode}{Character string specifying the travel mode used by
\code{\link[ggmap:trek]{ggmap::trek()}}. Common options include \code{"driving"}, \code{"walking"},
\code{"bicycling"}, and \code{"transit"}. Defaults to \code{"bicycling"}.}

\item{cache_dir}{Character string specifying the directory where the elevation
data will be cached.}
}
\value{
A data frame containing:
\describe{
\item{name}{Route identifier supplied via \code{name}.}
\item{lat}{Latitude coordinate of the route point.}
\item{lon}{Longitude coordinate of the route point.}
\item{distance}{Distance in meters from the previous route point. The
first value is 0.}
\item{cumulative_distance}{Cumulative distance in meters from the route
origin.}
\item{elevation}{Elevation in meters obtained from AWS Terrain Tiles.}
}
}
\description{
Generates a route between an origin and destination using Google Maps,
retrieves elevation values for each route point from AWS Terrain Tiles via
the \pkg{elevatr} package, and calculates segment and cumulative distances.
}
\details{
The function (1) retrieves route coordinates using \code{\link[ggmap:trek]{ggmap::trek()}}.
(2) Converts the route to an \code{sf} object.
(3) Obtains elevation values using \code{\link[elevatr:get_elev_point]{elevatr::get_elev_point()}} with \code{src = "aws"}.
(4) Computes geodesic distances between consecutive points using \code{\link[geosphere:distGeo]{geosphere::distGeo()}}.

Before using this function, register your Google Maps API key.
Set in your environment variable as e.g. "GOOGLE_MAPS_KEY" and then register:

\preformatted{
ggmap::register_google(
  key = Sys.getenv("GOOGLE_MAPS_KEY"),
  write = TRUE
)
}
}
\examples{
\dontrun{
route_elev <- get_elevation_data(
  name = "Bike Tour",
  origin = "Utrecht, Netherlands",
  destination = "Chaesalp, Switzerland",
  mode = "bicycling"
)

head(route_elev)
}

}
\seealso{
\code{\link[=plot_elevation]{plot_elevation()}}
}
