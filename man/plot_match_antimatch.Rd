% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_match_antimatch.R
\name{plot_match_antimatch}
\alias{plot_match_antimatch}
\title{Plot Match and Anti-Match Labels}
\usage{
plot_match_antimatch(
  name_match,
  name_antimatch,
  add_box = TRUE,
  text_color = "#235347",
  text_size = 15,
  margin_size = 10,
  ...
)
}
\arguments{
\item{name_match}{Character string  (e.g. Name) specifying the label for the "match" panel.}

\item{name_antimatch}{Character string (e.g. Name) specifying the label for the "anti-match" panel.}

\item{add_box}{Logical. If \code{TRUE} (default), a box (generated via
\code{plot_box()}) is drawn behind each label panel.}

\item{text_color}{Character. Color used for the label text. Defaults to
\code{"#235347"}.}

\item{text_size}{Numeric. Size of the label text. Defaults to \code{15}.}

\item{margin_size}{Numeric. Plot margin (applied equally to all four sides, in
points) for each panel. Defaults to \code{10}.}

\item{...}{Additional arguments passed to \code{box_box()} when
\code{add_box = TRUE}.}
}
\value{
A \pkg{patchwork} object combining the match and anti-match plots
stacked vertically.
}
\description{
Creates a two-panel stacked plot displaying a "match" label and an "anti-match" label
(both prefixed with an emoji), each optionally enclosed in a decorative box,
and combined vertically using \pkg{patchwork}.
}
\details{
The function builds a small data frame containing the match and
anti-match labels, then creates two separate \pkg{ggplot2} plots (one per
label) using \code{geom_text()} with \code{theme_void()}. If
\code{add_box = TRUE}, each label plot is inset onto a box plot produced
by \code{box_plot(...)} using \code{patchwork::inset_element()}. The two
resulting plots are then stacked vertically with \code{patchwork} using
equal heights.
}
